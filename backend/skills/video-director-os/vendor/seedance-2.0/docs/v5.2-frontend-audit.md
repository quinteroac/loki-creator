# Frontend Audit

## Findings from v5.1

- README rendered as compressed Markdown with long one-line tables.
- Root `SKILL.md` and scripts also appeared collapsed.
- GitHub Actions workflow was missing from `.github/workflows/`.
- Visual layer had a hero and skill map, but the information architecture was overloaded.

## v5.2 changes

- Clean GitHub README layout.
- New dark and light hero SVGs.
- New skill map SVG with grouped modules.
- Design audit script to prevent collapsed Markdown from returning.
